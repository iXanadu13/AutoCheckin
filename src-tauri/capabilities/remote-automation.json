{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-automation",
  "description": "Allows configured remote check-in pages to report completion only.",
  "windows": ["checkin-window"],
  "local": false,
  "remote": { "urls": ["http://*/*", "https://*/*"] },
  "permissions": ["automation"]
}